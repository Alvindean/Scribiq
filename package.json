{
  "name": "scribiq",
  "version": "1.0.0",
  "description": "Scribiq — AI Writing Studio",
  "private": true,
  "engines": {
    "node": ">=18.x"
  },
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  }
}
